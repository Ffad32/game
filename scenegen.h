#include <string.h>

void processNPCSquare(int suffix, char* text,int scenemun) {
    
    switch (suffix) {
        case 1:
            strcpy(text, "maps/map1.map");
            break;
        case 2:
            break;
        case 3:
            break;
        
        default:
            break;
    }
}