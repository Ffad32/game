#include <string.h>

int processNPCSquare(int suffix, char *text, int scenemun)
{

    switch (suffix)
    {
    case 1:
        switch(scenemun)
        {
        case 1:
            // start dialogue
            // return 1
            break;
        }
        strcpy(text, "maps/map1.map");
        break;
    case 2:
        break;
    case 3:
        break;

    default:
        break;
    }
}