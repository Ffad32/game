#include <string.h>

void processNPCSquare(int suffix, char* filename, char* picname) {
    
    switch (suffix) {
        case 1:
            break;
        case 2:
            break;
        case 3:
            break;
        
        default:
            break;
    }
}